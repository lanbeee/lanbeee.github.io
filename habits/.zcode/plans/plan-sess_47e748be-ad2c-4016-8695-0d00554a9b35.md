Modern flat + subtle glass/glow polish pass (styles.css only, plus sw.js cache bump)

All changes in /Users/nabeelkhan/Downloads/repo/lanbeee.github.io/habits. Vanilla CSS, no JS logic changes — visual quieting only (per your answer). Dark-mode variants included for every color/shadow change.

## 1. Restore the glowy + button (from old `tings/`)
- `.app-bar-add` and `.bottom-nav .nav-add`: add back the old glow `box-shadow:0 7px 18px color-mix(in srgb,var(--teal-icon) 28%,transparent)` (old file line 641), plus a slightly stronger hover/active glow (transition on box-shadow). Subtle, not neon.

## 2. Fix the add-sheet type selector merging with background
Root cause: sweep at styles.css:4650–4653 makes the seg well `--bg3` and the active option `--bg` — inside a `--bg` sheet, and with border:0, the active "habit/task" pill reads as a hole. Fix:
- Active `.seg-opt.on`: keep `--bg` fill but add a soft raised shadow (mini `--floating-shadow`-lite, e.g. `0 2px 6px rgba(20,30,26,.12)`) so it reads as a raised pill, not a cut-out. Keep font-weight bump.
- Deepen well slightly: `.seg{background:color-mix(in srgb,var(--bg3) 70%,var(--bg2))}` for a clearer tone step from the sheet.
- Global (affects all segs: type, priority, schedule, adjacency) — consistent and desirable.

## 3. Subtle glass/glow accents ("very subtle, where needed")
- Add a `--glass-bg` / `--glass-blur` token pair: topbar and bottom-nav get translucent surface (`color-mix(--bg 72%, transparent)`) + `backdrop-filter:blur(14px) saturate(1.4)` + existing `--floating-shadow`. Sheets keep solid bg (glass over content behind sheets reads badly on mobile).
- Soft accent glow on primary interactive elements only: + buttons (above), `.btn.primary` teal-family shadow (reuse `0 8px 24px color-mix(... 12%)` pattern at line 3818 where primary-colored), wordmark glow toned *down* from 18% → ~10% (less noise per your home request).
- `prefers-reduced-transparency`/no-backdrop fallback: solid `--bg`.

## 4. Finish the border removal (leftovers not covered by the 4572–4681 sweep)
Set `border:0` and replace with tonal edges/focus rings where visible, for: `.travel-edit-row .icon-btn` (2714), `.day-availability-preset` (3890), `.time-offset-sign-btn` (2221), `.mini-text-btn` (3272), `.day-quick-action,.day-item-action` (3850), `.ting-emoji-preview` (1974 — keep teal focus ring), `.generic-emoji-chip` (4518), `.stat` plain (1639), `.scheduled-row` (2341), `.monthday-disclosure-head` (2043), `.plan-field-row` (2376), `.blocked-time-row` (2462), `.location-access-row` (2512), `.add-option-group` (2905), `.day-scoped-habit` (2977), `.crown-dial` (3006), `.about-block` (3033), `.setting-toggle` (3110), `.calendar-pdf-preview` (3193), `.reset-confirm` (3243), `.day-empty-state` dashed (3841), `.day-move-inline` (3869), `.day-availability-hero` (3881), `.timer-auto-stop-field` (2771/2998), `.order-mark` (4479), `.home-active-filter` (3350), `.switch-ui` track (3129 — tone edge instead). Same pattern as the existing sweep: focus-visible keeps the teal `0 0 0 3px` ring; functional dividers become `--bg2`/`--bg3` tone steps.

## 5. Home-screen quieting (CSS only, info preserved)
- Meta pills: replace accent-family fills with quiet "ghost" pills — `--bg2` fill, `--text2` text, colored **dot only** for status/priority semantics (color stays as a small signal, fills go away). Hard-due red keeps a faint tinted fill (it's a real alert).
- Font weights: agenda pill 700→600, header pills 800→700 and reduce letter-spacing, section headers 720→650.
- Trail dots: reduce chroma (hit/warn/miss tones mixed ~55% toward `--bg3`), today ring thinner.
- Priority dot: smaller (6→4.5px) and lower opacity.
- Breakable bar: replace saturated `#12A66B/#7C5CFC/#F5871F` with token-mixed softer variants (green/purple/orange icons at ~70% over bg), thinner bar.
- Snoozed/quiet states unchanged.

## 6. Housekeeping
- Bump `sw.js` `const CACHE = 'tings-v114'` → `tings-v115'` (styles.css is precached — gotcha #2).
- Update the token comment block (~lines 30–40, 95–100) to describe the new glass/glow tokens (keep RN-port sync note accurate).
- Verify visually: `npx serve -l 4181`, screenshot home + add sheet + settings in light and dark; run `./run-tests.sh ui` (no JS changes expected, but confirms nothing asserted on styling classes broke).